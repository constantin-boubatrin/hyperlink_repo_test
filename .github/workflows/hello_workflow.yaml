name: hello_workflow

on: [push]

jobs:
  print-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
      uses: actions/checkout@v3
      - name: Hello world using echo
      run: echo "$(<hello.txt)"